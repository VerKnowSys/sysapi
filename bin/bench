#!/bin/sh
# blame: @dmilith
# 2018-11-22-0129-1542846591


. bin/build


_times=10
echo "Bench: SysAPI: Running measurement ${_times} times (sequentially one by one)."
for i in $(seq 0 ${_times}); do
    RUSTFLAGS="-lkvm -lprocstat" cargo run --bin kernelbench ${_cargo_release_flag}
done
